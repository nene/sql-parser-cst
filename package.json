{
  "name": "sql-parser-cst",
  "description": "Parses SQL into Concrete Syntax Tree (CST)",
  "license": "GPLv2",
  "main": "lib/parser.js",
  "types": "lib/parser.d.ts",
  "private": true,
  "scripts": {
    "pretty": "prettier --write .",
    "grammar": "ts-node pegjs/generate.ts",
    "build": "yarn grammar && tsc",
    "test:mysql": "jest --config test/config/mysql.json",
    "test:sqlite": "jest --config test/config/sqlite.json",
    "test": "yarn grammar && yarn test:mysql && yarn test:sqlite"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "peggy": "^2.0.1",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
