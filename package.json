{
  "name": "sql-parser-cst",
  "description": "Parses SQL into Concrete Syntax Tree (CST)",
  "main": "lib/parser.js",
  "types": "lib/parser.d.ts",
  "private": true,
  "scripts": {
    "pretty": "prettier --write .",
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  },
  "jest": {
    "preset": "ts-jest",
    "roots": [
      "test"
    ],
    "moduleNameMapper": {
      "^\\.(.+)\\.js": ".$1"
    }
  }
}
