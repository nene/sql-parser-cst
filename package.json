{
  "name": "sql-parser-cst",
  "description": "Parses SQL into Concrete Syntax Tree (CST)",
  "license": "GPLv2",
  "main": "lib/parser.js",
  "types": "lib/parser.d.ts",
  "private": true,
  "scripts": {
    "pretty": "prettier --write .",
    "grammar": "peggy pegjs/mysql.pegjs",
    "build": "yarn grammar && tsc",
    "test": "yarn grammar && jest"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "peggy": "^2.0.1",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  },
  "jest": {
    "preset": "ts-jest",
    "roots": [
      "test"
    ]
  }
}
